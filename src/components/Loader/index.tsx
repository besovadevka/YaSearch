import React, { FC } from 'react';
import { LoaderWrapper } from './styled';

export const Loader: FC = () => {
  return (
    <LoaderWrapper>
      <p>
        <span>Ya</span>Search
      </p>
    </LoaderWrapper>
  );
};
